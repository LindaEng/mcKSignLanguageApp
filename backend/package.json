{
	"name": "backend",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"dependencies": {
		"pg": "^8.7.1",
		"pg-hstore": "^2.3.4",
		"sequelize": "^6.12.5"
	},
	"devDependencies": {
		"sequelize-cli": "^6.3.0",
		"nodemon": "^2.0.7"
	},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"make-model": "npx sequelize-cli model:generate --name alphabet  --attributes alphabet:string,image:string",
		"migrate": "npx sequelize-cli db:migrate",
		"down": "npx sequelize-cli db:migrate:undo",
		"make-seed-data": "npx sequelize-cli seed:generate --name demo-alphabet",
		"seedDB":"npx sequelize-cli db:seed:all"
	},
	"author": "",
	"license": "ISC"
}
